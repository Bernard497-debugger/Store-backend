services:
  - type: web
    name: khali-app-store
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true
    disk:
      name: db-disk
      mountPath: /var/data
      sizeGB: 1